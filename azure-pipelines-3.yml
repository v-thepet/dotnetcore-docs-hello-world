trigger: 
- main

pool: 
   vmImage: ubuntu-latest

jobs:
- deployment: VMDeploy
  displayName: Deploy to VM
  environment: env2025-linux
  strategy:
     runOnce:
        deploy:   
          steps:
            - script: echo "Hello world"
